---
name: agency-seo-optimizer
description: Overhauls website structure and metadata for advanced, professional SEO targeting paid advertising and content marketing clients.
---

### Goal
To structure the Next.js routing and UI layers to rank highly for competitive digital marketing keywords, ensuring perfect semantic HTML and rich search results.

### Instructions
1. Analyze the current DOM structure of the target page for semantic correctness (proper hierarchical use of `<header>`, `<main>`, `<section>`, `<h1>` to `<h6>`).
2. Generate and inject comprehensive Schema.org JSON-LD markup for a `ProfessionalService` named "Times Digital Media". Ensure it includes services offered (Paid Ads, Content Marketing).
3. Refactor Next.js `generateMetadata` exports (or standard `<head>` blocks) to include high-converting title tags (max 60 characters) and meta descriptions (max 160 characters).
4. Audit all `next/image` components or standard `<img>` tags to ensure they possess descriptive, keyword-rich `alt` attributes.

### Constraints
- Do not use generic keywords like "best marketing agency." Use precise, technical terminology (e.g., "Meta Ads Bid Strategies," "SEO Audits").
- Do not alter the visual rendering of the page during this step; only modify the semantic structure and head metadata.